const firstName="Saurabh";
const lastName="Jumnalkar";
const country="India";
const state="Maharashtra";
const courseCount=0;
const isLoggedInFromGoogle=false;
const isLoggedInFromFacebook=true;

console.log(firstName);

console.log(`
    Name of User: ${firstName} ${lastName}
    User lives in:${country},${state}
    his coursecount is ${courseCount}
    Logged from Google:${isLoggedInFromGoogle}
    Logged from facebook:${isLoggedInFromFacebook}
`);